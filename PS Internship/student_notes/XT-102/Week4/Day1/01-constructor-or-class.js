function Person(name,age){
    this['first name']= name;
    this.age= age;
}

const bindu= new Person('bindu',22);
console.log(Bindu);

// __proto__ - to inherit properties from another  (Establish relationship between object to object)