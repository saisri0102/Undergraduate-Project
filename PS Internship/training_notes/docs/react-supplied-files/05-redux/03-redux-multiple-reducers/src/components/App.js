import React from 'react';
import Counter from './Counter';

export default function App( props ) {
    return (
        <React.Fragment>
            <Counter />
            {/* add UserForm element */}
        </React.Fragment>
    );
}