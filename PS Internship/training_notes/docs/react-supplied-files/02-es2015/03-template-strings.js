/**
 * Strings can be quoted using backticks (``) apart from single and double quotes
 */
// "hello"
// 'hello'

let message = 'Hello', name = 'world';

// Step 1: Create a greeting message using backtick quoted strings and log it

// Step 2: Create the markup for an HTML table with a row that shows details of James
let james = {
    name: 'James',
    age: 32,
    email: 'james@zeolearn.com'
};

// --------------------------------------------

// Self-exploration: You can also call functions within ${}, eg. ${fn()}. The functionis called when the string is created and the returned value is used.